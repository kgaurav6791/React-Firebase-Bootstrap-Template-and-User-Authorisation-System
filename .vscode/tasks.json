{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            
            "label": "gittask",
            "type": "shell",
            "command": "git add . ; git commit -m 'updated with git task' ; git push -u origin main ;"
        }
    ]
}